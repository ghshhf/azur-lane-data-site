{
  "name": "azur-lane-data-site",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "fetch:official": "node scripts/fetch-official.mjs",
    "validate": "node scripts/validate-data.mjs",
    "prebuild": "node scripts/validate-data.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "test": "npm run validate && npm run build && npm run test:engine && npm run test:smoke",
    "test:smoke": "vite build --ssr scripts/smoke/entry.jsx --outDir .smoke --emptyOutDir --logLevel warn && node scripts/smoke.mjs",
    "test:engine": "vite build --ssr scripts/engine/entry.js --outDir .engine --emptyOutDir --logLevel warn && node scripts/test-engine.mjs"
  },
  "dependencies": {
    "lucide-react": "^0.441.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.13",
    "vite": "^5.4.8"
  }
}
